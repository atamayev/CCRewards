import { StyleSheet } from "react-native"

const CommonStyles = StyleSheet.create({
	mainContainer: {
		flex: 1,
		backgroundColor: "#fff",
	},
	pageView: {
		position: "absolute",
		top: 30,
		left: 20
	},
	headerText: {
		fontWeight: "bold",
		fontSize: 30,
	}
})

export default CommonStyles
